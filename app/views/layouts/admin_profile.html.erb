<%= content_for :app do %>

    <%= render :partial => 'layouts/profile_content', :locals => {
      :side => "admin-side",
      :right_bar => false
    } %>
    <!-- /#wrapper -->
<% end %>
<%= render :template => 'layouts/application' %>