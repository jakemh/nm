
<%= content_for :app do %>

    <%= render :layout => 'layouts/profile_content', 
        :locals => {:side => "user-side", :right_bar => true} do %>
      <%= yield %>

        <div id="nm-root">
         <script type="text/x-handlebars" data-template-name="application">
            {{outlet me}}
      </script> 

    </div>
    <% end %>
    <!-- /#wrapper -->
  <% end %>

  <%= render :template => 'layouts/application' %>
