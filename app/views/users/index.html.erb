<h2>Users</h2>
<ul>
  <% for user in @users %>
    <li>
      <%=h user.first_name %>
      <%= link_to "Add Friend", me_connections_path(:connect_to_id => user.id, :type => "Friendship"), :method => :post %>

    </li>
  <% end %>
</ul>
