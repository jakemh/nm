<div ng-init="SideBar.profExternalInit()" class="center-text">
  <div class="profile__box-title">
    {{delegate.entity.first_name}}'s Businesses
  </div>

  <div class="bus__list">
    <div ng-repeat="business in delegate.userBusinesses | orderBy:'name': false ">
      <div ng-include="'business_item.html'"></div>
   <!--  <div class="bus__none" ng-hide="displayList.length">
      Find some more businesses!
    </div> -->
  </div>
</div>

</div>
