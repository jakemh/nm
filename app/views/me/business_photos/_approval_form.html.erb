  <%= form_for([:me, @business], :html => { :multipart => true }) do |f| %>
     <%= f.hidden_field :profile_photo_id, :value => @photo.id %>
     <%= f.submit "ACCEPT PHOTO", :class => "button__form button__form--full" %>
   
    <% end %>