# App.provider "currentUser", ->
  
#   # In the provider function, you cannot inject any
#   # service or factory. This can only be done at the
#   # "$get" method.
#   # @_user = "DEFAULT"

#   @$get = ->
#     user: user
#     setCurrentUser: (user) =>
#       user = user
   

  

#   return
